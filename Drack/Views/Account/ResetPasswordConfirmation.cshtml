@{
    ViewBag.Title = "Reset password confirmation";
}

<div class="row">
    <div class="col-md-12">
        <section id="account-form">
            <div class="row text-center">
                <div class="col-sm-12">
                    <h3 class="font-weight-light text-secondary m-2">@ViewBag.Title</h3>
                    <p>
                        <i class="fas fa-user-lock icon-lg text-primary"></i>
                    </p>
                </div>
            </div>

            <p class="font-weight-light text-center text-primary">
                Your password has been reset. Please @Html.ActionLink("click here to log in", "Login", "Account", routeValues: null, htmlAttributes: new { @class = "btn btn-outline-primary", id = "loginLink" })
            </p>
        </section>
    </div>
</div>